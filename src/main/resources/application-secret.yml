steam:
  api:
    key: {YOUR_STEAM_API_KEY} # 务必使用你的真实 Key
    # baseUrl: https://api.steampowered.com # 默认已经是这个

# application.yml
spring:
  ai:
    dashscope:
      api-key: {YOUR_DASH_SCOPE_API_KEY}
      chat:
        options:
          model: qwen3-max-2026-01-23

search-api:
  base-url: https://www.searchapi.io/api/v1/search?engine=google
  api-key: {YOUR_SEARCH_API_KEY}